import { FilterArticuloPipe } from './filter-articulo.pipe';

describe('FilterArticuloPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArticuloPipe();
    expect(pipe).toBeTruthy();
  });
});
